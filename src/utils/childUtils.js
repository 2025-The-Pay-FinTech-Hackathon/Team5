// 자녀 통계 반환 더미 함수
export const getChildStats = (userId) => {
  // 실제로는 localStorage 등에서 불러오거나, 아래는 예시 데이터
  return {
    savingsGoal: 50000,
    currentSavings: 20000,
    completedMissions: 3,
    totalMissions: 5,
  };
}; 